# Test Database Configuration
# Copy this file to .env and update the values for your PostgreSQL instance

# Test database URL for integration tests
# Format: postgres://username:password@host:port/database?sslmode=disable
TEST_DATABASE_URL=postgres://pgarrow_test:pgarrow_test_password@localhost:5432/pgarrow_test?sslmode=disable

# Note: Integration tests will create isolated schemas for each test,
# so you don't need to worry about test data cleanup between runs.